#!/usr/bin/env bash

SCRIPTDIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SRCDIR="$(dirname "$SCRIPTDIR")"
IMGSRCDIR="$SRCDIR/img"
LIBSRCDIR="$SRCDIR/lib"

LOCALBASE="$HOME/Library/texmf/tex/latex"

cp -p $SRCDIR/dndbook.cls $LOCALBASE
cp -p $SRCDIR/dnd-phb.sty $LOCALBASE
cp -p $SRCDIR/dndcore.def $LOCALBASE
cp -p $SRCDIR/dndoptions.clo $LOCALBASE
cp -pR $IMGSRCDIR $LOCALBASE
cp -pR $LIBSRCDIR $LOCALBASE
